// export data
const userInfo = {"name":"adam", "age":28};

const printUserInfo = (name, age) => {
    return name + " 사용자의 나이는 " + age + "세 입니다.";
}

const grade = "나이 합격";

export {userInfo, grade, printUserInfo};